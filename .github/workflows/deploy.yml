name: GitHub CI/CD

on:
  push:
    branches:
      - '*'

jobs:
  deploy:
    uses: MohistMC/devops-framework/.github/workflows/deploy.yml@develop
    with:
      version: ${{ github.ref_name }}-${{ github.sha }}
    secrets: inherit
