name: GitHub CI/CD

on:
  push:
    branches:
      - '*'

jobs:
  deploy:
    uses: MohistMC/devops/.github/workflows/deploy.yml@1.0.20
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
